# 🚀 Руководство по первому запуску UE5 + Rust Integration

**Версия**: v0.6.0
**Дата**: 2026-02-16
**Для**: Tower Game - После успешной компиляции

---

## ✅ Предварительные требования

Перед началом убедитесь:
- ✅ Visual Studio 2022 установлена
- ✅ UE5 проект скомпилирован успешно
- ✅ tower_core.dll (1.9 MB) в `Binaries/Win64/`
- ✅ Output Log показывает: "Tower Rust Core initialized. Version: 0.6.0"

---

## 🧪 ТЕСТ 1: Проверка версии Rust Core (1 минута)

### Цель
Убедиться что DLL загружена и работает

### Шаги

#### 1. Создать тестовый Blueprint Actor

**Content Browser** (внизу редактора):
1. Правый клик в пустой области
2. **Blueprint Class** → **Actor**
3. Назовите: `BP_TestRustVersion`
4. Двойной клик для открытия

#### 2. Создать логику проверки версии

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Game Instance]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓ (Return Value)
[Is Rust Core Ready?]
    ↓ (True branch)
[Get Core Version]
    ↓ (Return Value)
[Print String] (Duration: 10.0)
```

**Настройка узлов**:
- **Get Subsystem**: Class = `TowerGameSubsystem`
- **Print String**: Text Color = Green

#### 3. Разместить на уровне

1. **Compile** Blueprint
2. **Save**
3. Перетащите `BP_TestRustVersion` из Content Browser на уровень
4. Нажмите **Play** (Alt + P)

#### 4. Проверить результат

**В левом верхнем углу экрана**:
```
0.6.0
```

**В Output Log**:
```
LogTemp: Tower Rust Core initialized. Version: 0.6.0
```

✅ **Если видите "0.6.0"** → Интеграция работает!

---

## 🎲 ТЕСТ 2: Генерация процедурного этажа (3 минуты)

### Цель
Проверить генерацию этажей через Rust

### Создать Blueprint для генерации

**Создайте новый Actor**: `BP_TestFloorGeneration`

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Game Instance]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓
[Generate Floor Layout]
    - Tower Seed: 42
    - Floor ID: 5
    ↓ (Return Value: String)
[Print String] (Duration: 15.0)
```

### Ожидаемый результат

**На экране появится JSON**:
```json
{
  "floor_id": 5,
  "width": 50,
  "height": 50,
  "tiles": [...],
  "rooms": [...]
}
```

✅ **Если видите JSON** → Процедурная генерация работает!

---

## ⚔️ ТЕСТ 3: Расчёт урона (5 минут)

### Цель
Проверить боевую систему

### Создать Blueprint: `BP_TestCombat`

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Game Instance]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓
[Calculate Damage]
    - Base Damage: 100.0
    - Attack Angle: 45.0
    - Combo Stage: 0
    - Weapon Name: "sword"
    - Action Type: "slash"
    ↓ (Return Value: String)
[Print String] (Duration: 15.0)
```

### Ожидаемый результат

```json
{
  "final_damage": 120,
  "angle_multiplier": 1.2,
  "combo_multiplier": 1.0,
  "semantic_bonus": 0,
  "hit_type": "Normal"
}
```

**Проверка**:
- `angle_multiplier`: 1.2 (45° = оптимальный угол атаки)
- `final_damage`: 120 (100 × 1.2)

✅ **Если видите правильный урон** → Боевая система работает!

---

## 🔥 ТЕСТ 4: Hot-Reload конфигурации (v0.6.0)

### Цель
Проверить систему горячей перезагрузки

### Создать Blueprint: `BP_TestHotReload`

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Game Instance]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓
[Get Hot Reload Status]
    ↓ (Return Value)
[Print String] (Text Color: Cyan, Duration: 10.0)
    ↓
[Delay] (Duration: 2.0)
    ↓
[Trigger Config Reload]
    ↓ (Return Value: Int32)
[Print String] ("Reloaded X configs")
```

### Действия

1. Запустите Blueprint
2. Увидите: `{"enabled": true, "watch_interval": 5.0}`
3. Через 2 секунды: `"Reloaded 4 configs"` (количество может отличаться)

### Экспериментируйте

1. Откройте `config/game_state.json`
2. Измените значение (например, `tick_rate: 60` → `tick_rate: 120`)
3. Нажмите **Trigger Config Reload** снова
4. Изменения применены без перезапуска! ✅

---

## 📊 ТЕСТ 5: Система аналитики (v0.6.0)

### Цель
Проверить запись и чтение событий

### Создать Blueprint: `BP_TestAnalytics`

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Game Instance]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓
[Branch 1] Record Damage Dealt
    - Weapon Name: "Longsword"
    - Amount: 150
    ↓
[Branch 2] Record Floor Cleared
    - Floor ID: 5
    - Tier: 1
    - Time (seconds): 347.5
    ↓
[Branch 3] Record Gold Earned
    - Amount: 1250
    ↓
[Delay] (Duration: 0.5)
    ↓
[Get Analytics Snapshot]
    ↓
[Print String] (Duration: 20.0)
```

### Ожидаемый результат

```json
{
  "total_events": 3,
  "events": [
    {"type": "DamageDealt", "weapon": "Longsword", "amount": 150},
    {"type": "FloorCleared", "floor_id": 5, "tier": 1, "time": 347.5},
    {"type": "GoldEarned", "amount": 1250}
  ]
}
```

✅ **Если видите все 3 события** → Аналитика работает!

---

## 🎮 ТЕСТ 6: Визуализация процедурного этажа (15 минут)

### Цель
Отрисовать этаж на основе данных от Rust

### Подготовка

Вам понадобятся:
- Базовые меши: Cube, Plane (из UE5 Starter Content)
- Материалы: M_Floor, M_Wall (можно создать простые)

### Создать Blueprint: `BP_FloorVisualizer`

**Components**:
- Добавьте `Scene` компонент (Root)

**Event Graph**:

```
[Event BeginPlay]
    ↓
[Get Subsystem (TowerGameSubsystem)]
    ↓
[Generate Floor Layout] (Seed: 42, Floor: 1)
    ↓ (JSON String)
[Parse JSON] (custom function)
    ↓
[For Each Tile in JSON]
    ↓
[Spawn Actor (StaticMeshActor)]
    - Location: (Tile.X * 300, Tile.Y * 300, 0)
    - Mesh: Based on Tile.Type
```

### Создать функцию Parse JSON

**Inputs**: JSON String
**Outputs**: Array of Tile Structs

**В функции используйте**:
- `Json Object` библиотека UE5
- `Get Field` для извлечения tiles массива

### Результат

После запуска:
- На уровне появятся кубики/плоскости
- Расположение соответствует JSON данным
- Каждый тип тайла имеет свой меш/цвет

✅ **Если видите сгенерированный этаж** → Визуализация работает!

---

## 🐛 Типичные проблемы и решения

### ❌ "Failed to initialize Tower Rust Core"

**Причины**:
1. DLL не найдена в `Binaries/Win64/`
2. Старая версия DLL (не 1.9 MB)
3. Отсутствуют экспорты (не 100 функций)

**Решение**:
```bash
# Проверить DLL
ls -lh ue5-client/Binaries/Win64/tower_core.dll
# Ожидается: 1.9 MB

# Если нет - скопировать новую
cp [path-to-ci-dll] ue5-client/Binaries/Win64/
```

---

### ❌ "Get Subsystem" возвращает null

**Причина**: TowerGameSubsystem не инициализирована

**Решение**:
1. Проверьте Output Log на ошибки инициализации
2. Убедитесь что DLL загрузилась
3. Попробуйте перезапустить редактор

---

### ❌ Blueprint узлы не находятся

**Причина**: Код не скомпилирован или модуль не загружен

**Решение**:
1. Закройте UE5
2. Откройте `.uproject` снова
3. Пересоберите модуль (Yes при запросе)
4. Перезапустите редактор

---

### ❌ JSON пустой или невалидный

**Причина**: Rust функция вернула ошибку

**Решение**:
1. Проверьте параметры (Floor ID > 0, Seed > 0)
2. Проверьте Output Log на Rust ошибки
3. Попробуйте другие параметры

---

## 📋 Чек-лист успешной интеграции

После выполнения всех тестов:

- [ ] ✅ Версия "0.6.0" отображается
- [ ] ✅ Генерация этажей возвращает валидный JSON
- [ ] ✅ Расчёт урона работает корректно
- [ ] ✅ Hot-Reload перезагружает конфиги
- [ ] ✅ Аналитика записывает события
- [ ] ✅ Визуализация отрисовывает этаж

**Если все пункты ✅** → **Интеграция полностью работает!** 🎉

---

## 🚀 Следующие шаги

После успешного тестирования:

### 1. Создать игровой уровень
- Процедурная генерация в реальном времени
- Навигация между этажами
- Система спавна монстров

### 2. Подключить Nakama
- Запустить сервер: `docker-compose up -d`
- Настроить мультиплеер
- Синхронизация состояния

### 3. Реализовать боевую систему
- Non-target combat с физикой
- Анимации атак
- Система комбо

### 4. Добавить UI
- HUD с здоровьем/энергией
- Инвентарь
- Миникарта

---

## 📚 Дополнительные ресурсы

**Документация**:
- `docs/UE5_INTEGRATION_TEST.md` - Полные тесты интеграции
- `docs/INTEGRATION_STATUS_REPORT.md` - Отчёт о состоянии
- `docs/INTEGRATION_PROGRESS.md` - История решения проблем

**Примеры кода**:
- `Source/TowerGame/Bridge/ProceduralCoreBridge.h` - FFI мост
- `Source/TowerGame/Core/TowerGameSubsystem.h` - Blueprint API

**Конфигурация**:
- `config/engine.json` - Настройки движка
- `procedural-core/Cargo.toml` - Rust настройки

---

**Версия**: 1.0
**Автор**: Claude Sonnet 4.5
**Дата**: 2026-02-16

**Удачи с первым запуском!** 🎮🚀
